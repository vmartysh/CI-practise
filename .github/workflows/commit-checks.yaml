name: 'PR title and body check'
on:
  pull_request:
    types:
      - opened
      - edited
      - reopened
      - synchronize


jobs:
  check-commit-message:
    name: Check Commit Message
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
    outputs:
      res: ${{ steps.check.outputs.result}}
    steps:

      - id: check
        run: |
              TITLE='^.{13,72}$'
              BODY='AB#[0-9]{6,10}'

              if [[ "${{ github.event.pull_request.title }}" =~ $TITLE && "${{ github.event.pull_request.body }}" =~ $BODY ]]; then
                echo "result=0" >> "$GIRHUB_OUTPUT"
              else
                echo "result=1" >> "$GIRHUB_OUTPUT"
                exit 1
              fi


  pr_comment:
    if: ${{ needs.check-commit-message.outputs.res == 0}}
    runs-on: ubuntu-latest
    needs: check-commit-message
    permissions:
      pull-requests: write
    name: PR comment with reactions
    steps:
      - id: pr
        uses: thollander/actions-comment-pull-request@v2
        with:
          message: |
                    Hello world ! :wave:
          reactions: eyes, rocket

