name: Steps output
on:
  workflow_dispatch:

jobs:
  stepjob:
    runs-on: ubuntu-latest

    steps:
      - id: step1
        run: |
            echo Success!
            exit 1
      - id: step2 
        run: |
            echo Failed!
            exit 1

